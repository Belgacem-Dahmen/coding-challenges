/**
 * return a volume of cube
 * @param {number} l 
 * @param {number} w 
 * @param {number} h 
 * @returns {number} v
 * 
 */


function getVolume(l, w, h) {
    return l * w * h
}

console.log(getVolume(1, 2, 2));
